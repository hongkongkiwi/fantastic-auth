{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "vault-server/Dockerfile"
  },
  "deploy": {
    "startCommand": "./vault-server",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
