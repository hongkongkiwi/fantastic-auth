# DigitalOcean App Platform

Use `app.yaml` and fill in secrets in DO's UI or `doctl apps`.
See `ops/deployments/RECOMMENDATIONS.md` for DB/Redis/HA guidance.

## Provider Notes
- Postgres: DigitalOcean Managed PostgreSQL.
- Redis: DigitalOcean Managed Redis.
